--- origsrc/webkit-sharp/configure.ac	2008-06-23 19:10:37.000000000 -0500
+++ src/webkit-sharp/configure.ac	2008-06-23 19:11:12.812500000 -0500
@@ -37,22 +37,35 @@
 PKG_CHECK_MODULES(MONO_DEPENDENCY, mint >= 1.0, has_mint=true, has_mint=false)
 
 if test "x$has_mono" = "xtrue"; then
-	if test `uname -s` = "Darwin"; then
+	case `uname -s` in
+	Darwin)
 		AC_PATH_PROG(RUNTIME, mono, no)
 		AC_PATH_PROG(CSC, mcs, no)
-		LIB_PREFIX=
+		LIB_PREFIX=lib
+		LIB_MIDDLE=.
 		LIB_SUFFIX=.dylib
-	else
+		;;
+	CYGWIN*)
+		AC_PATH_PROG(RUNTIME, mono, no)
+		AC_PATH_PROG(CSC, mcs, no)
+		LIB_PREFIX=cyg
+		LIB_MIDDLE=-
+		LIB_SUFFIX=.dll
+		;;
+	*)
 		AC_PATH_PROG(RUNTIME, mono, no)
 		AC_PATH_PROG(CSC, mcs, no)
-		LIB_PREFIX=.so
+		LIB_PREFIX=lib
+		LIB_MIDDLE=.so.
 		LIB_SUFFIX=
-	fi
+		;;
+	esac
 else
 	if test "x$has_mint" = "xtrue"; then
 		AC_PATH_PROG(RUNTIME, mint, no)
 		AC_PATH_PROG(CSC, mcs, no)
-		LIB_PREFIX=.so
+		LIB_PREFIX=lib
+		LIB_MIDDLE=.so.
 		LIB_SUFFIX=
 	else
 		AC_PATH_PROG(CSC, csc.exe, no)
@@ -60,7 +73,8 @@
 			AC_MSG_ERROR([You need to install either mono or .Net])
 		else
 			RUNTIME=
-			LIB_PREFIX=
+			LIB_PREFIX=lib
+			LIB_MIDDLE=.
 			LIB_SUFFIX=.dylib
 		fi
 	fi
@@ -83,6 +97,7 @@
 AC_SUBST(GACUTIL)
 AC_SUBST(GACUTIL_FLAGS)
 AC_SUBST(LIB_PREFIX)
+AC_SUBST(LIB_MIDDLE)
 AC_SUBST(LIB_SUFFIX)
 
 dnl Check for gtk-sharp
--- origsrc/webkit-sharp/samples/Makefile.am	2008-06-23 19:10:36.000000000 -0500
+++ src/webkit-sharp/samples/Makefile.am	2008-06-23 19:11:12.828125000 -0500
@@ -16,7 +16,7 @@
 all: $(TARGETS)
 
 $(assembly): ../sources/$(assembly)
-	cp ../sources/$(ASSEMBLY_NAME).dll $(srcdir)
+	cp ../sources/$(ASSEMBLY_NAME).dll .
 
 funnybrowser.exe: $(assembly) $(srcdir)/FunnyBrowser.cs
 	$(CSC)  /out:$@ $(references) $(srcdir)/FunnyBrowser.cs
--- origsrc/webkit-sharp/sources/AssemblyInfo.cs.in	2008-06-23 19:10:36.000000000 -0500
+++ src/webkit-sharp/sources/AssemblyInfo.cs.in	2008-06-23 19:11:12.843750000 -0500
@@ -8,4 +8,4 @@
 [assembly: AssemblyCopyright("@ASSEMBLY_COPYRIGHT@")]
 [assembly: AssemblyVersion("@ASSEMBLY_VERSION@")]
 [assembly: AssemblyDelaySign(false)]
-[assembly: AssemblyKeyFile("@ASSEMBLY_NAME@.snk")]
+[assembly: AssemblyKeyFile("@srcdir@/@ASSEMBLY_NAME@.snk")]
--- origsrc/webkit-sharp/sources/Makefile.am	2008-06-23 19:10:36.000000000 -0500
+++ src/webkit-sharp/sources/Makefile.am	2008-06-23 19:11:12.859375000 -0500
@@ -46,10 +46,10 @@
 $(API): $(srcdir)/$(RAW_API) $(srcdir)/$(METADATA)
 	cp $(srcdir)/$(RAW_API) $(API)
 	chmod u+w $(API)
-	$(GAPI_FIXUP) --api=$(srcdir)/$(API) --metadata=$(srcdir)/$(METADATA)
+	$(GAPI_FIXUP) --api=$(API) --metadata=$(srcdir)/$(METADATA)
 
 generated-stamp: $(API)
-	 $(GAPI_CODEGEN) --generate $(srcdir)/$(API) $(GTK_SHARP_CFLAGS) \
+	 $(GAPI_CODEGEN) --generate $(API) $(GTK_SHARP_CFLAGS) \
 	 	--outdir=generated --customdir=$(srcdir) \
 	 	--assembly-name=$(ASSEMBLY_NAME) && touch generated-stamp
 
--- origsrc/webkit-sharp/sources/webkit-sharp.dll.config.in	2008-06-23 19:10:36.000000000 -0500
+++ src/webkit-sharp/sources/webkit-sharp.dll.config.in	2008-06-23 19:11:12.875000000 -0500
@@ -1,3 +1,3 @@
 <configuration>
-  <dllmap dll="webkit-1.0" target="webkit-1.0@LIB_PREFIX@.1@LIB_SUFFIX@"/>
+  <dllmap dll="webkit-1.0" target="@LIB_PREFIX@webkit-1.0@LIB_MIDDLE@1@LIB_SUFFIX@"/>
 </configuration>
